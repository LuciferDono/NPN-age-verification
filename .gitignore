# data + weights — never committed, they are large and the dataset is Kaggle-licensed
data/
checkpoints/
*.pt
*.pth
*.onnx

# the dataset's own archives
*.zip

# runtime
server/npn.db
server/_selfcheck.db
__pycache__/
*.py[cod]
.venv/
venv/

# frontend
web/node_modules/
web/dist/
web-purple/node_modules/
web-purple/dist/
web-purple/tsconfig.tsbuildinfo
web/.vite/

# demo recordings. Large binaries that change every take, so they live locally and
# are shared as files rather than through git history.
docs/demo/
demo/raw/
*.mp4.tmp

.env
.DS_Store

# ad-hoc probe images used while testing by hand. Real people, web-sourced, no
# consent or licence on file — they must not enter a public biometric repo.
tests/*.jpg
tests/*.jpeg
tests/*.png

# personal notes and memos. Never committed, never shared.
notes/

# built decks. Large binaries that change on every rebuild, and the generator in deck/
# reproduces them from source, so they are shared as files rather than through history.
docs/*.pptx

# Exploratory dashboard concepts. Scratch work for picking a direction — never pushed.
docs/temp/
